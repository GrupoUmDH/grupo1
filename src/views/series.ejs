<%- include('partials/header') %>
  
    
    <main>
        <section class="container">
            <div class="titulos">
                <h1>Séries</h1>
            </div>

            <hr>
            <section class="series-home">
                <% for(const serie of series) { %>
                <div>
                    <img src="/img/series/<%=serie.imagem%>.jpg">
                    <p><%=serie.nome%></p>
                    <div class="preco"><span> <%=serie.valor%></span></div>
                </div>
                <% }%>                                    
            </section>
        </section>
    </main>       


<%- include('partials/footer')%>

