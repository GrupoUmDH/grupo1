<%- include('partials/header') %>
    <main>

        <section class="container">
            <div class="titulos">
                <h1>Séries</h1>
            </div>

            <hr>
            <section class="series-home">
                <% for(const listaSeries of series) { %>
                    <div class="series-show">
                        <a href="<%= '/produtos' %>\">
                            <img src="/img/series/<%=listaSeries.background%>.jpg" alt="" id="back-series">
                            <div class="descri">
                                <div>
                                    <h3 id="nome-series">
                                        <%=listaSeries.nome%>
                                    </h3>
                                    <p id="descri-series">
                                        <%= listaSeries.descricao %>
                                    </p>
                                </div>
                                <div id="dados-series">
                                    <h1 id="id-series">
                                        <%= listaSeries.id %>
                                    </h1>
                                    <h1 id="categoria-series">
                                        <%= listaSeries.id %>
                                    </h1>
                                    <h1 id="tipo-series">
                                        <%= listaSeries.tipo %>
                                    </h1>
                                    <h1 id="valor-series">
                                        <%= listaSeries.valor%>
                                    </h1>
                                    <img src="/img/series/<%= listaSeries.imagem %>.jpg" id="img-series">
                                    <%= listaSeries.imagem %>
                                </div>
                            </div>
                        </a>
                    </div>
                    <% }%>
            </section>
        </section>
    </main>
<%- include('partials/footer')%>