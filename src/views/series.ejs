<%- include('partials/header') %>
  
    
    <main>
        <section class="container">
            <div class="titulos">
                <h1>Séries</h1>
            </div>

            <hr>
            <section class="series-home">
                <% for(const serie of series) { %>
                <div>
                    <img src="/img/series/<%=serie.img%>.jpg">
                    <p><%=serie.nome%></p>
                    <div class="preco"><span><%=serie.valor_aluguel%></span></div>
                    <div class="preco"><span><%=serie.valor_compra%></span></div>
                </div>
                <% }%>                                    
            </section>
        </section>
    </main>       


<%- include('partials/footer')%>

