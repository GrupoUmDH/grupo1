const path = require('path');

module.exports = {
    config: path.resolve(__dirname, './config/config.json'), 
    'models-path': path.resolve(__dirname, './models')
}

// ligo as configurações do meu banco 